import java.util.Random;

public class main {
    Random gerador = new Random();
    //private static final String[] tipos_herois = {Corrupted_IA, };

    //Class Heroi = tipos_herois[gerador.nextInt(tipos_herois.length)];

    //private int heroi_escolhido = gerador.nextInt(2);
    

};
